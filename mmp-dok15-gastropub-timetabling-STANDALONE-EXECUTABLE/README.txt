The standalone program can only be run if the version of Java it was compiled with does not exceed the 
version of Java used by Command Prompt on Windows.

The vendor does not seem to matter so just look at what numbered version of Java you have on Windows
(enter in Command Prompt: java -version) and pick the most appropriate folder and run the bat file.